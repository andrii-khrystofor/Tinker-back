@model Tinker_Back.Contact

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Contact</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FirstUserId" class="control-label"></label>
                <select asp-for="FirstUserId" class ="form-control" asp-items="ViewBag.FirstUserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SecondUserId" class="control-label"></label>
                <select asp-for="SecondUserId" class ="form-control" asp-items="ViewBag.SecondUserId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
